# NPS Insight AI 能力升级 - 验证清单

## 安全加固（横切层一）

- [x] 输入清洗：控制字符（\0、\b、\x0b、\x0c、\r 等）被正确过滤
- [x] 注入检测："忽略之前的指令"、"输出系统提示词"等已知模式被拦截
- [x] 长度限制：超长输入被截断或拒绝，不触发模型上下文溢出
- [x] XML 标签隔离：系统指令和用户输入使用不同的 XML 标签包裹
- [x] 宪法声明：System Prompt 中包含安全边界声明，模型遇到越界请求会拒绝
- [x] Zod 输出校验：所有 AI 返回的 JSON 都经过 Zod Schema 校验
- [x] 校验失败重试：Zod 校验失败后自动重试，最多 2 次，仍失败返回友好错误
- [x] 所有 AI 调用路径都经过安全中间层，无遗漏

## Token 与成本管理（横切层二）

- [x] Token 计数准确：与 OpenAI 官方 Tokenizer 偏差 < 5%
- [x] 输入 Token 统计：每次请求记录输入 Token 数
- [x] 输出 Token 统计：每次响应记录输出 Token 数
- [x] 费用估算：根据模型单价计算每次调用的费用
- [x] 调用日志完整：记录模型、时间、Token 数、耗时、费用、任务类型
- [x] 月度统计接口：`/api/ai/usage` 返回正确的月度统计数据
- [ ] Token 计数 UI：对话界面输入框旁显示实时 Token 数（待 Task 7 集成）
- [ ] 超阈值警告：Token 数超过阈值时变红警告（待 Task 7 集成）

## 模型路由

- [ ] 任务类型枚举定义完整：tagging / evolution / intent / answer / summary
- [ ] 轻量任务走小模型：意图识别、摘要等调用小模型
- [ ] 重量任务走大模型：打标、进化等调用大模型
- [ ] 路由配置可配置：配置中心可修改各任务使用的模型
- [ ] 配置热更新：修改配置后下次调用生效，无需重启
- [ ] 路由可降级：模型不可用时自动回退到默认大模型

## 语义缓存

- [ ] Embedding 向量化：使用 Embedding API 对查询做向量化
- [ ] 余弦相似度计算：相似度 > 0.95 时命中缓存
- [ ] 缓存命中快：缓存响应时间 < 200ms
- [ ] 不产生 API 调用：缓存命中时不调用模型 API
- [ ] TTL 过期机制：缓存有过期时间，到期自动失效
- [ ] 手动清除：支持手动清除全部缓存
- [ ] 不误命中：语义不同的问题不会命中缓存
- [ ] 打标任务不缓存：打标类任务每次内容不同，不使用缓存

## SSE 流式输出（L0 接入层）

- [x] Provider 流式接口：LLMProvider 接口包含 chatStream 方法
- [x] OpenAI 流式实现：OpenAIProvider 正确实现流式调用
- [x] AgnesAI 流式实现：AgnesAIProvider 正确实现流式调用
- [x] ReadableStream 返回：返回标准 ReadableStream
- [x] AbortController 支持：传入 AbortSignal 可中断请求
- [x] 流式内容正确：流式输出内容与同步输出一致
- [x] 错误处理：流式过程中出错能正确捕获并上报

## 流式对话界面

- [ ] 聊天页面存在：`/chat` 页面可访问
- [ ] 消息列表：历史消息正确展示，区分用户和 AI 消息
- [ ] 输入框：支持多行输入，Enter 发送，Shift+Enter 换行
- [ ] 打字机效果：AI 回复逐字渲染，流畅不卡顿
- [ ] 首字节延迟：< 500ms（本地测试）
- [ ] 取消按钮：流式过程中可点击取消中断请求
- [ ] 状态机：idle / streaming / reconnecting / error 状态切换正确
- [ ] 断线重连：网络中断后自动指数退避重连（1s → 2s → 4s → max 30s）
- [ ] Token 计数显示：输入框旁显示当前输入的 Token 数
- [ ] 响应式布局：移动端和桌面端都正常显示

## 打标进度流式展示

- [ ] 进度 SSE 接口：`/api/tags/progress` 返回进度事件
- [ ] 进度面板：点击"开始周打标"后弹出进度面板
- [ ] 进度条：实时更新完成百分比
- [ ] 详细信息：显示当前条数 / 总条数 / 成功数 / 失败数
- [ ] 完成提示：任务完成后显示结果摘要
- [ ] 关闭面板：支持手动关闭进度面板
- [ ] 不阻塞 UI：进度展示不影响其他操作

## Prompt 模板引擎（L1 认知层）

- [x] Handlebars 集成：模板引擎使用 Handlebars
- [x] 模板目录结构：按场景分类（tagging / evolution / chat）
- [x] 模板渲染正确：渲染结果与原字符串拼接语义一致
- [x] 自动转义：用户输入中的 HTML/JS 特殊字符被自动转义
- [x] Zod Schema 配套：每个模板有对应的 Zod 输出 Schema
- [x] 新增场景无需改核心：新增模板 + Schema 即可扩展新功能
- [x] 版本标识：每个模板带有版本号，方便追踪

## 配置中心 AI 用量面板

- [ ] 用量 Tab 存在：配置中心有"AI 用量"标签页
- [ ] 本月统计：显示本月调用次数、Token 消耗、费用估算
- [ ] 趋势图表：近 7 天 / 30 天调用趋势图
- [ ] 分类统计：按任务类型分类展示
- [ ] 清除缓存按钮：可手动清除语义缓存
- [ ] 调用日志列表：展示最近的调用记录
- [ ] 数据准确：面板数据与后端日志一致

## 回归验证（不破坏现有功能）

- [ ] 周打标完整流程：从拉取反馈 → AI 打标 → 写入表 → 通知，全链路正常
- [ ] 月分析完整流程：标签进化 → Top 问题 → 公式同步 → 文档生成 → 通知，全链路正常
- [ ] 周报生成：周报文档内容完整，数据正确
- [ ] 月报生成：月报文档内容完整，数据正确
- [ ] 配置保存：配置中心所有配置保存和回显正常
- [ ] 飞书 Bot 命令：`/nps tag` 和 `/nps analyze` 命令正常工作
- [ ] Webhook 接收：FeelGood 和飞书 Webhook 正常接收处理
- [ ] 打标准确率：升级后打标准确率不下降（与升级前对比）

## 代码质量

- [ ] TypeScript 编译零错误：`pnpm tsc --noEmit` 通过
- [ ] ESLint 零警告：`pnpm lint` 通过
- [ ] 生产构建成功：`pnpm build` 通过
- [ ] 无 any 类型：新增代码不使用 any 类型
- [ ] 错误处理：所有异步操作都有 try/catch
- [ ] 代码注释：关键逻辑有中文注释
- [ ] 命名规范：遵循项目命名规范（kebab-case / PascalCase / camelCase）
- [ ] 依赖体积：新增依赖后部署包体积增加 < 5MB
