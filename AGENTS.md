---
alwaysApply: true
description: "项目全局规则，所有 AI 任务必须遵守"
priority: 10
---

# 全局规则

## 语言
- 所有回复使用中文
- 代码注释使用中文

## 编码底线
- 禁止使用 any 类型
- 禁止提交硬编码的密码、密钥或 Token
- 所有用户输入必须经过校验

## 不猜测规则
- **不猜测用户意图**: 需求不明确时，必须先向用户确认，不能凭直觉假设
- **不猜测配置值**: 必须从环境变量或 KV 存储读取，禁止硬编码配置
- **不猜测 API 响应格式**: 必须查阅官方文档，禁止假设接口返回结构
- **不猜测数据库结构**: 必须从实际数据或 schema 文件读取，禁止假设字段名

## 工作习惯
- 修改代码前先阅读现有代码，理解上下文
- 每次只修改一个功能/修复一个 Bug，不要混合
- 修改完成后不要立马结束，需要对变动的代码进行审查，确保完成了所有功能以及没有引入新问题
- 完成后输出变更摘要：改了什么、为什么改、影响范围
